[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"
  NEXT_DISABLE_NETLIFY_PLUGIN = "true"  # 🔥 Force-disable Netlify's Next.js runtime

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
